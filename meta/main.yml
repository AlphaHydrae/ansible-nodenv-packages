galaxy_info:
  author: Simon Oulevay (Alpha Hydrae)
  description: Installs a global npm package with nodenv.
  license: MIT
  min_ansible_version: 2.0

  platforms:
    - name: Darwin
      versions:
        - all

  galaxy_tags:
    - node
    - npm
    - nodenv

allow_duplicates: yes

dependencies:
  - role: AlphaHydrae.multipass
  - role: AlphaHydrae.nodenv
